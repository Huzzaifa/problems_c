#include <stdio.h>
#include <cs50.h>

int main(void)
{

// Find Sum of ith powers of first n integers, where i can be any number.

    /*int n = get_int("N: ");
    int l = get_int("Power: ");
    int sum = 0;
    int sq[n];
    sq[0] = 0;
    for (int i = 0; i < n ; i++ )
    {
        sum = sum + sq[i];
        for(int k = 1; k < n; k++ )
        {

            int z = k;
            int y = k;

            for(int j = 1; j < l ; j++)
            {
                y = y * z;
                if (j == l-1)
                {
                    sq[k] = y;
                }
            }

        }
        printf("%i\n",sq[i]);

    }
    printf("Sum: %i\n",sum);
}*/